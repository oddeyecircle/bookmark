import { BookTitleTrimPipe } from './book-title-trim.pipe';

describe('BookTitleTrimPipe', () => {
  it('create an instance', () => {
    const pipe = new BookTitleTrimPipe();
    expect(pipe).toBeTruthy();
  });
});
